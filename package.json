{
  "name": "llm-proxy-key",
  "version": "0.0.1",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon app.ts",
    "build": "tsc"
  },
  "keywords": [],
  "author": "xscode",
  "license": "MIT",
  "description": "A multi-provider AI API key proxy with round-robin rotation. Routes requests to different upstream providers based on URL path prefix.",
  "dependencies": {
    "@types/express": "^5.0.3",
    "@types/node": "^24.3.0",
    "dotenv": "^17.2.1",
    "express": "^5.1.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "nodemon": "^3.1.10",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.2"
  }
}
